import { ValidaDataMenorDirective } from './valida-data-menor.directive';

describe('ValidaDataMenorDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidaDataMenorDirective();
    expect(directive).toBeTruthy();
  });
});
